syntax = "proto3";

import "buf/validate/validate.proto";

option csharp_namespace = "DotProtoValidate.Messages";

message User {
  int32 age = 1 [(buf.validate.field).cel = {
    id: "user.age",
    expression: "this < 18 ? 'User must be at least 18 years old': ''"
  }];
}
